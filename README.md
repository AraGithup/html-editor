# HTML 编辑器

一个纯前端、无需安装的 HTML 可视化编辑器，直接在浏览器中运行。

## 使用方式

直接打开 `index.html`，或通过 GitHub Pages 访问：
**https://aragithub.github.io/html-editor/**

## 功能

### 上传
- **上传 HTML**：单个 `.html` 文件
- **上传文件夹**：含图片、视频等资源的完整项目，资源自动转为 base64 内嵌
- 支持拖拽上传（文件和文件夹均可）
- Chrome / Edge 下使用 File System Access API，上传时获取可写句柄，支持直接覆盖原文件

### 文字编辑
- 直接点击页面任意文字即可编辑
- 工具栏支持：加粗、斜体、下划线、删除线、字号、字体颜色、对齐方式

### 图片 / 视频替换
- 鼠标悬停图片或视频，点击「🔄 替换」按钮上传新文件

### 链接 / 按钮编辑
- **直接点击**：触发跳转（链接导航 / 按钮 onclick）
- **悬停 800ms 后点击**：进入文字编辑模式（出现蓝色虚线框提示）
- 悬停时出现「🔗 编辑链接」按钮，点击可修改 `href` 和 `onclick`

### 多页面 / 页面内导航
- 上传文件夹时自动识别所有 HTML 文件
- 工具栏 ◀ ▶ 箭头支持两种导航：
  - **跨文件跳转**：页面内链接点击自动跳转到对应 HTML 文件
  - **页面内状态**：单页应用每次按钮点击后自动记录 DOM 快照，◀ 可逐步返回之前的页面状态

### 保存
- **保存副本**：将所有页面的编辑内容保存为带时间戳的副本
  - 上传文件夹时，**所有页面**（包括未访问过的）均会一并下载，每页独立文件
  - 可选择是否将资源内嵌为单文件（base64）或还原为相对路径
- **更新原稿**：直接覆盖原文件（Chrome/Edge，需 File System Access API 授权）；不支持时降级为下载

## 技术说明

- 纯原生 HTML + JS，无任何依赖
- 编辑在 `<iframe>` 内进行，通过 `postMessage` 与父页面通信
- 编辑内容按文件名独立缓存（`edits[filename]`），跨页跳转不丢失任何页面的改动
- 保存时在 iframe 内 clone body、剥离编辑器注入的按钮和 wrapper，只保留用户的真实改动
- 资源文件通过 `FileReader` 转为 base64，保存时可选还原为相对路径
